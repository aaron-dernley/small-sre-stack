apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "prima-api.fullname" . }}-config
  labels:
    {{- include "prima-api.labels" . | nindent 4 }}
data:
  {{- range $key, $val := .Values.env }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}
